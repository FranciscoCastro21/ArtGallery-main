<html>
  <head>
    <title>{{title}}</title>

    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;600&display=swap" rel="stylesheet">

    <style>
     
      body {
        font-family: 'Poppins', sans-serif;
      }

      /* Style for the title */
      .h1body {
        font-size: 3rem; /* Larger font size for elegance */
        font-weight: 600; /* Semi-bold for emphasis */
        color: #333; /* Dark color for text */
      }

      p {
        font-size: 1.2rem; /* Increase the font size for better readability */
        color: #555; /* Slightly lighter text color */
      }

      .btn-dark {
        font-size: 1.1rem; /* Slightly larger text for the button */
        padding: 10px 20px; /* Adding some padding for a better button appearance */
        border-radius: 30px; /* Rounded button edges for a smoother look */
      }
    </style>
  </head>
  <body class="bg-light">
    <!-- Main Content -->
    <div class="container mt-5">
      <h1 class="h1body">{{title}}</h1>
      <p>Welcome to {{title}}</p>
      <p>The #1 place to get Artworks in Canada!</p>
      <p><a href="/artworks" class="btn btn-dark">Browse Artworks</a></p>
    </div>
  </body>
</html>
